package model;
//Daria pra fazer o objetivo com o enum já que é constante, mas vamos usar inteface
public interface Objetivo {

    //vou dividir em perda de peso(emagrecimento), ganho de massa muscular, manter
    double getObjetivo();
}
